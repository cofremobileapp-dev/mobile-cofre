# Environment Configuration for Cofre Mobile App
# Copy this file to .env.local for local development
# Copy to .env.production for production builds

# ========================================
# LOCAL DEVELOPMENT API URL
# ========================================
# Get your computer's IP address:
# Windows: ipconfig (look for IPv4 Address)
# Mac/Linux: ifconfig or ip addr show
#
# Example: If your IP is 192.168.1.7 and Laravel runs on port 8000:
LOCAL_API_URL=http://192.168.1.7:8000/api

# ========================================
# PRODUCTION API URL
# ========================================
# IMPORTANT: Set this to your actual production backend URL
# This will be used when building for production (eas build)
#
# Examples:
# PRODUCTION_API_URL=https://api.cofre.app/api
# PRODUCTION_API_URL=https://cofre-backend.herokuapp.com/api
# PRODUCTION_API_URL=https://your-ec2-instance.compute.amazonaws.com/api
PRODUCTION_API_URL=https://your-production-domain.com/api

# ========================================
# BUILD CONFIGURATION
# ========================================
# Environment: development | staging | production
ENVIRONMENT=development

# Enable debug mode (logs, warnings)
DEBUG_MODE=true

# ========================================
# FEATURE FLAGS (Optional)
# ========================================
# Enable/disable features for testing
ENABLE_ANALYTICS=false
ENABLE_CRASHLYTICS=false
ENABLE_PUSH_NOTIFICATIONS=true

# ========================================
# NOTES
# ========================================
# 1. NEVER commit .env.local or .env.production to git
# 2. Add them to .gitignore
# 3. For EAS builds, configure secrets via:
#    eas secret:create --name PRODUCTION_API_URL --value https://your-api.com
# 4. Backend MUST use HTTPS in production for security
